{
  "name": "vue3_learn",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "push": "git add . && git commit -m '组件生命周期' && git push -u origin  master"
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}