// 1.Set对象是值的集合，你可以按照插入的顺序迭代它的元素。 Set中的元素只会出现一次，即 Set 中的元素是唯一的。
// 2.NaN和undefined都可以

// 创建Set数据结构：
// const setData = new Set(param)
// 其中，param是一个具有iterable接口的数据解构，用来初始化Set数据结构









// Set实例属性：
// 1.Set.prototype.size：返回Set对象中值的个数

// Set实例方法：
// 1.Set.prototype.add(value)：在Set对象尾部添加一个元素，返回该Set对象

// 2.Set.prototype.clear()：移除Set对象内的所有元素


















